name = "bolt-cyberstrike"
main = "worker.js"
compatibility_date = "2024-01-01"

# Custom domain - add in Cloudflare Dashboard after deploy
# routes = [
#   { pattern = "bolt.cyberstrike.io", custom_domain = true }
# ]
