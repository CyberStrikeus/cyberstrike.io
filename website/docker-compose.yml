version: '3.8'

services:
  landing:
    build:
      context: ./landing
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped

  docs:
    build:
      context: ./docs
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    restart: unless-stopped
